{
  "name": "@mythic-forge/android",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor Android shell for Mythic Forge. Hosts the editor build from apps/editor/dist.",
  "type": "module",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:debug": "cap sync android && cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
