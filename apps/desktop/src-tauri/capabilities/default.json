{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions: the editor needs no native commands in 0.1.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
